# ===========================================
# DOT Project 환경 설정 (설치 시 자동 생성)
# ===========================================

# 이 PC의 역할에 따라 MASTER_IP 설정:
# - Master 서버: 이 PC의 IP 주소
# - Worker 서버: Master PC의 IP 주소
MASTER_IP=__MASTER_IP__

# Redis/Backend URL (자동 설정됨)
REDIS_URL=redis://__MASTER_IP__:6379/0
BACKEND_URL=http://__MASTER_IP__:8000

# 데이터베이스 설정
DB_USER=root
DB_PASSWORD=12345
DB_NAME=aidot_db

# Worker용 DB 계정
WORKER_DB_USER=aidot_user
WORKER_DB_PASSWORD=1234

# Redis DB 번호
REDIS_DB=0

# CORS 설정 (Master IP로 자동 설정됨)
ALLOW_ORIGINS=http://localhost:5173,http://__MASTER_IP__:5173

# Vite 설정 (비워두면 브라우저 hostname 사용)
VITE_API_URL=

# 업로드 경로 (Master 전용)
UPLOAD_PATH=/app/uploads

# ===========================================
# 이미지 생성 설정
# ===========================================
# 번역 모드: llm (LLM 활용), local (경량 모델)
# 폐쇄망에서는 llm 사용
TRANSLATION_MODE=llm

# ===========================================
# 폐쇄망 설정 (HuggingFace 오프라인 모드)
# ===========================================
HF_HOME=/ai_models/embedding
TRANSFORMERS_CACHE=/ai_models/embedding
HF_HUB_OFFLINE=1
